<Window x:Class="CyberSecurityChatBot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Bot"
        Height="700"
        Width="1000"
        Background="#0F172A" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d">

    <Grid Margin="15">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- HEADER -->
        <Border Background="#111827"
                CornerRadius="15"
                Padding="15"
                Margin="0,0,0,15">

            <StackPanel>

                <TextBlock Text="Cipher Chat Bot"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="#22C55E"
                           HorizontalAlignment="Center"/>

                <TextBlock x:Name="AsciiArtText"
                           FontFamily="Consolas"
                           FontSize="12"
                           Foreground="LightGreen"
                           TextAlignment="Center"
                           Margin="0,15,0,0"/>
            </StackPanel>
        </Border>

        <!-- CHAT AREA -->
        <Border Grid.Row="1"
                Background="#1E293B"
                CornerRadius="15"
                Padding="10">

            <ScrollViewer VerticalScrollBarVisibility="Auto">

                <TextBlock x:Name="ChatArea"
                           Foreground="White"
                           FontSize="16"
                           TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- INPUT SECTION -->
        <Grid Grid.Row="2"
              Margin="0,15,0,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="140"/>
            </Grid.ColumnDefinitions>

            <!-- TEXTBOX -->
            <TextBox x:Name="UserInput"
                     Height="45"
                     FontSize="16"
                     Background="#334155"
                     Foreground="White"
                     BorderThickness="0"
                     Padding="10"
                     VerticalContentAlignment="Center"
                     KeyDown = "UserInput_KeyDown"/>

            <!-- BUTTON -->
            <Button Grid.Column="1"
                    Content="SEND"
                    Margin="10,0,0,0"
                    FontSize="16"
                    FontWeight="Bold"
                    Background="#22C55E"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="SendButton_Click"/>
            
            
        </Grid>
    </Grid>
</Window>